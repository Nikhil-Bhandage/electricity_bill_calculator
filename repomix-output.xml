This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
index.html
package.json
script.js
server.js
style.css
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="index.html">
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>PowerGrid AI • Smart Electricity Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
</head>
<body class="text-white">

<div class="animated-bg">
    <div class="blob blob1"></div>
    <div class="blob blob2"></div>
</div>

<div class="max-w-7xl mx-auto px-6 py-10">

    <!-- HERO -->
    <div class="text-center mb-10">
        <div class="inline-flex items-center gap-3 px-5 py-2 rounded-full glass mb-6">
            <div class="w-3 h-3 bg-emerald-400 rounded-full animate-pulse"></div>
            <span class="text-sm font-semibold text-slate-300">AI Powered Energy Intelligence</span>
        </div>
        <h1 class="text-6xl font-black tracking-tight mb-4">
            Smart Electricity <span class="bg-gradient-to-r from-indigo-400 to-cyan-400 bg-clip-text text-transparent">Dashboard</span>
        </h1>
        <p class="text-slate-400 text-lg max-w-2xl mx-auto">
            Analyze appliance usage, predict monthly bills, and track smart savings in real time.
        </p>
    </div>

    <!-- TOP METRICS -->
    <div class="grid lg:grid-cols-4 md:grid-cols-2 gap-6 mb-8">
        <div class="glass metric-card rounded-3xl p-6">
            <p class="text-slate-400 text-sm">Monthly Units</p>
            <h2 id="unitsMetric" class="text-4xl font-black mt-3">0</h2>
            <p class="text-emerald-400 text-sm mt-2">kWh consumed</p>
        </div>
        <div class="glass metric-card rounded-3xl p-6">
            <p class="text-slate-400 text-sm">Estimated Bill</p>
            <h2 id="billMetric" class="text-4xl font-black mt-3">₹0</h2>
            <p class="text-indigo-400 text-sm mt-2">Including taxes</p>
        </div>
        <div class="glass metric-card rounded-3xl p-6">
            <p class="text-slate-400 text-sm">Efficiency Score</p>
            <div class="flex items-center gap-4 mt-4">
                <div class="w-12 h-12 rounded-full bg-indigo-500 flex items-center justify-center font-bold">82</div>
                <div><h3 class="font-bold">Excellent</h3><p class="text-xs text-slate-400">Optimized</p></div>
            </div>
        </div>
        <div class="glass metric-card rounded-3xl p-6">
            <p class="text-slate-400 text-sm">AI Insight</p>
            <div class="mt-4 space-y-1">
                <p class="text-xs text-yellow-300">⚡ AC consumes 42% power</p>
                <p class="text-xs text-emerald-300">💡 Save ₹650 using LEDs</p>
            </div>
        </div>
    </div>

    <div class="grid lg:grid-cols-3 gap-8">
        <!-- LEFT CONTENT -->
        <div class="lg:col-span-2 space-y-8">
            <!-- BILLING CONFIG -->
            <div class="glass rounded-3xl p-8">
                <h2 class="text-2xl font-bold mb-6">Billing Configuration</h2>
                <div class="grid md:grid-cols-4 gap-5">
                    <div>
                        <label class="text-xs uppercase text-slate-500 font-bold">Days</label>
                        <input id="days" type="number" value="30" class="input mt-2 w-full rounded-2xl p-4">
                    </div>
                    <div>
                        <label class="text-xs uppercase text-slate-500 font-bold">Rate / Unit</label>
                        <input id="rate" type="number" value="8" class="input mt-2 w-full rounded-2xl p-4">
                    </div>
                    <div>
                        <label class="text-xs uppercase text-slate-500 font-bold">Fixed Charge</label>
                        <input id="fixed" type="number" value="150" class="input mt-2 w-full rounded-2xl p-4">
                    </div>
                    <div>
                        <label class="text-xs uppercase text-slate-500 font-bold">Tax %</label>
                        <input id="tax" type="number" value="5" class="input mt-2 w-full rounded-2xl p-4">
                    </div>
                </div>
            </div>

            <!-- APPLIANCES -->
            <div class="glass rounded-3xl p-8">
                <div class="flex justify-between items-center mb-8">
                    <div>
                        <h2 class="text-2xl font-bold">Appliance Intelligence</h2>
                        <p class="text-slate-400 text-sm mt-1">Track usage and power consumption</p>
                    </div>
                    <button onclick="addNewAppliance()" class="gradient-btn px-6 py-3 rounded-2xl font-bold">
                        + Add Appliance
                    </button>
                </div>
                <div id="cards" class="space-y-5">
                    <!-- Cards injected by JS -->
                </div>
            </div>
        </div>

        <!-- RIGHT SIDEBAR -->
        <div class="space-y-8">
            <div class="glass rounded-3xl p-8 sticky top-10">
                <h2 class="text-2xl font-bold mb-6">Live Bill</h2>
                <div class="space-y-4">
                    <div class="flex justify-between"><span class="text-slate-400">Energy Cost</span><span id="energyCost" class="font-bold">₹0</span></div>
                    <div class="flex justify-between"><span class="text-slate-400">Fixed Charges</span><span id="fixedCost" class="font-bold">₹0</span></div>
                    <div class="flex justify-between border-b border-slate-800 pb-4"><span class="text-slate-400">Tax & Duty</span><span id="taxCost" class="font-bold">₹0</span></div>
                    <div class="pt-4">
                        <p class="text-xs uppercase text-indigo-400 font-black">Total Payable</p>
                        <h1 id="finalBill" class="text-6xl font-black mt-3 bg-gradient-to-r from-indigo-400 to-cyan-400 bg-clip-text text-transparent">₹0</h1>
                    </div>
                </div>
                <div class="mt-8">
                    <canvas id="chart"></canvas>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="script.js"></script>
</body>
</html>
</file>

<file path="package.json">
{
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mysql2": "^3.22.4"
  }
}
</file>

<file path="script.js">
const API_URL = "http://localhost:3000/api";
let chart;

// 1. Initial Load: Fetch data from MySQL
async function init() {
    try {
        // Get Config
        const configRes = await fetch(`${API_URL}/config`);
        const config = await configRes.json();
        if (config) {
            document.getElementById('days').value = config.days || 30;
            document.getElementById('rate').value = config.rate || 8;
            document.getElementById('fixed').value = config.fixed_charge || 150;
            document.getElementById('tax').value = config.tax || 5;
        }

        // Get Appliances
        const appRes = await fetch(`${API_URL}/appliances`);
        const appliances = await appRes.json();
        
        const container = document.getElementById('cards');
        container.innerHTML = '';
        
        if (appliances.length > 0) {
            appliances.forEach(app => renderApplianceUI(app));
        }
        
        calculate();
    } catch (err) {
        console.error("Connection to server failed. Make sure server.js is running.");
    }
}

// 2. Render a card into the UI
function renderApplianceUI(app) {
    const html = `
    <div class="glass rounded-3xl p-6 appliance-card border border-white/5" data-id="${app.id}">
        <div class="grid md:grid-cols-5 gap-4">
            <div>
                <label class="text-[10px] uppercase font-bold text-slate-500">Device Name</label>
                <input type="text" class="name input mt-1 w-full rounded-xl p-3 text-sm" value="${app.name}" oninput="save(${app.id})">
            </div>
            <div>
                <label class="text-[10px] uppercase font-bold text-slate-500">Watts</label>
                <input type="number" class="power input mt-1 w-full rounded-xl p-3 text-sm" value="${app.power}" oninput="save(${app.id})">
            </div>
            <div>
                <label class="text-[10px] uppercase font-bold text-slate-500">Hrs/Day</label>
                <input type="number" class="hours input mt-1 w-full rounded-xl p-3 text-sm" value="${app.hours}" oninput="save(${app.id})">
            </div>
            <div>
                <label class="text-[10px] uppercase font-bold text-slate-500">Qty</label>
                <input type="number" class="qty input mt-1 w-full rounded-xl p-3 text-sm" value="${app.qty}" oninput="save(${app.id})">
            </div>
            <div class="flex items-end">
                <button onclick="remove(${app.id})" class="w-full bg-red-500/10 hover:bg-red-500/20 text-red-400 rounded-xl py-3 text-sm font-bold transition">Remove</button>
            </div>
        </div>
    </div>`;
    document.getElementById('cards').insertAdjacentHTML('beforeend', html);
}

// 3. Add New (Database First)
async function addNewAppliance() {
    const newApp = { name: "New Device", power: 100, hours: 5, qty: 1 };
    
    const res = await fetch(`${API_URL}/appliances`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(newApp)
    });
    const result = await res.json();
    
    renderApplianceUI({ id: result.id, ...newApp });
    calculate();
}

// 4. Save to Database & Recalculate
async function save(id) {
    const card = document.querySelector(`[data-id="${id}"]`);
    const data = {
        id: id,
        name: card.querySelector('.name').value,
        power: parseFloat(card.querySelector('.power').value) || 0,
        hours: parseFloat(card.querySelector('.hours').value) || 0,
        qty: parseFloat(card.querySelector('.qty').value) || 0
    };

    await fetch(`${API_URL}/appliances`, {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(data)
    });

    calculate();
}

// 5. The Main Calculation Logic
function calculate() {
    const days = parseFloat(document.getElementById('days').value) || 0;
    const rate = parseFloat(document.getElementById('rate').value) || 0;
    const fixed = parseFloat(document.getElementById('fixed').value) || 0;
    const taxP = parseFloat(document.getElementById('tax').value) || 0;

    const cards = document.querySelectorAll('.appliance-card');
    let totalUnits = 0;
    let labels = [];
    let values = [];

    cards.forEach(card => {
        const p = parseFloat(card.querySelector('.power').value) || 0;
        const h = parseFloat(card.querySelector('.hours').value) || 0;
        const q = parseFloat(card.querySelector('.qty').value) || 0;
        const n = card.querySelector('.name').value || "Device";

        const units = (p * h * q * days) / 1000;
        totalUnits += units;
        
        labels.push(n);
        values.push(units);
    });

    const energyCost = totalUnits * rate;
    const taxAmt = (energyCost + fixed) * (taxP / 100);
    const grandTotal = energyCost + fixed + taxAmt;

    // Update UI
    document.getElementById('unitsMetric').innerText = totalUnits.toFixed(1);
    document.getElementById('billMetric').innerText = "₹" + Math.round(grandTotal);
    document.getElementById('energyCost').innerText = "₹" + energyCost.toFixed(2);
    document.getElementById('fixedCost').innerText = "₹" + fixed.toFixed(2);
    document.getElementById('taxCost').innerText = "₹" + taxAmt.toFixed(2);
    document.getElementById('finalBill').innerText = "₹" + Math.round(grandTotal);

    updateChart(labels, values);
}

// 6. Delete
async function remove(id) {
    await fetch(`${API_URL}/appliances/${id}`, { method: 'DELETE' });
    document.querySelector(`[data-id="${id}"]`).remove();
    calculate();
}

// 7. Config Auto-Save
['days', 'rate', 'fixed', 'tax'].forEach(id => {
    document.getElementById(id).addEventListener('input', async () => {
        const config = {
            days: document.getElementById('days').value,
            rate: document.getElementById('rate').value,
            fixed: document.getElementById('fixed').value,
            tax: document.getElementById('tax').value
        };
        await fetch(`${API_URL}/config`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(config)
        });
        calculate();
    });
});

// 8. Chart Update
function updateChart(labels, values) {
    const ctx = document.getElementById('chart').getContext('2d');
    if (chart) chart.destroy();
    chart = new Chart(ctx, {
        type: 'doughnut',
        data: {
            labels: labels,
            datasets: [{
                data: values,
                backgroundColor: ['#6366f1', '#14b8a6', '#f59e0b', '#ef4444', '#a855f7'],
                borderWidth: 0
            }]
        },
        options: {
            plugins: { legend: { display: true, position: 'bottom', labels: { color: '#94a3b8', font: { size: 10 } } } }
        }
    });
}

// Start
init();
</file>

<file path="server.js">
const express = require('express');
const mysql = require('mysql2');
const cors = require('cors');
const path = require('path');

const app = express();
const PORT = 3000;

// Middleware
app.use(cors());
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// Serve static files (index.html, style.css, script.js) from the current folder
app.use(express.static(__dirname));

// Database Connection
const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',      // Default XAMPP username
    password: '',      // Default XAMPP password (leave empty)
    database: 'power_grid'
});

db.connect(err => {
    if (err) {
        console.error('Database connection failed: ' + err.stack);
        return;
    }
    console.log('Connected to MySQL Database: power_grid');
});

// --- ROUTES ---

// 1. Serve the Home Page
app.get('/', (req, res) => {
    res.sendFile(path.join(__dirname, 'index.html'));
});

// 2. Get Billing Configuration
app.get('/api/config', (req, res) => {
    db.query('SELECT * FROM config WHERE id = 1', (err, results) => {
        if (err) return res.status(500).send(err);
        res.json(results[0]);
    });
});

// 3. Update Billing Configuration
app.post('/api/config', (req, res) => {
    const { days, rate, fixed, tax } = req.body;
    const query = 'UPDATE config SET days = ?, rate = ?, fixed_charge = ?, tax = ? WHERE id = 1';
    db.query(query, [days, rate, fixed, tax], (err) => {
        if (err) return res.status(500).send(err);
        res.json({ message: 'Configuration updated' });
    });
});

// 4. Get All Appliances
app.get('/api/appliances', (req, res) => {
    db.query('SELECT * FROM appliances', (err, results) => {
        if (err) return res.status(500).send(err);
        res.json(results);
    });
});

// 5. Add or Update Appliance
app.post('/api/appliances', (req, res) => {
    const { id, name, power, hours, qty } = req.body;

    if (id) {
        // Update existing
        const query = 'UPDATE appliances SET name = ?, power = ?, hours = ?, qty = ? WHERE id = ?';
        db.query(query, [name, power, hours, qty, id], (err) => {
            if (err) return res.status(500).send(err);
            res.json({ status: 'updated' });
        });
    } else {
        // Add new
        const query = 'INSERT INTO appliances (name, power, hours, qty) VALUES (?, ?, ?, ?)';
        db.query(query, [name, power, hours, qty], (err, result) => {
            if (err) return res.status(500).send(err);
            res.json({ id: result.insertId });
        });
    }
});

// 6. Delete Appliance
app.delete('/api/appliances/:id', (req, res) => {
    const id = req.params.id;
    db.query('DELETE FROM appliances WHERE id = ?', [id], (err) => {
        if (err) return res.status(500).send(err);
        res.json({ status: 'deleted' });
    });
});

// Start Server
app.listen(PORT, () => {
    console.log(`Server is running at http://localhost:${PORT}`);
});
</file>

<file path="style.css">
body {
    background: radial-gradient(circle at top left,#312e81 0%,transparent 35%), #020617;
    min-height: 100vh;
    font-family: 'Inter', sans-serif;
}
.glass {
    background: rgba(15, 23, 42, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(18px);
}
.input {
    background: rgba(15, 23, 42, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: white;
}
.gradient-btn {
    background: linear-gradient(135deg,#6366f1,#8b5cf6);
}
/* ... animations from your original code ... */
</file>

</files>
